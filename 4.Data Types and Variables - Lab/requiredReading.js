function requiredReading(bookPages, pagesForOneHour, daysHeHas) {
    let totalTimeToRead = bookPages / pagesForOneHour
    let requiredReading = totalTimeToRead / daysHeHas
    console.log(requiredReading);
} requiredReading
    (
        212,
        20,
        2
    )